import { Calendar } from 'react-date-range';

const CalendarComp = () => {

    return(
        <Calendar
        date={new Date()}
        onChange={() => {}}
      />
    )


}

export default CalendarComp